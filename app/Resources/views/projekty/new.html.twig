{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nowy projekt</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" />
    {{ form_end(form) }}

    <!--<ul>
        <li>
            <a href="{{ path('projekty_index') }}">Back to the list</a>
        </li>
    </ul>-->
{% endblock %}
