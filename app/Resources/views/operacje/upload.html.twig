{% extends 'base.html.twig' %}

{% block body %}
    <h1>Wyciagibankowe upload</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Załaduj" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('wyciagibankowe_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
